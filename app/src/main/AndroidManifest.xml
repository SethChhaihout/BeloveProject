<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.planbcambodia.beloveproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.BlockOrReport" />
        <activity android:name=".activity.CompanyProfile" />
        <activity android:name=".activity.ChatActivity" />
        <activity android:name=".activity.SelectLanguageActivity" />
        <activity android:name=".activity.PrivacyPolicyActivity" />
        <activity android:name=".activity.AboutMeActivity"></activity>
        <activity android:name=".activity.BlockedUsersActivity">

        </activity>
        <activity android:name=".UnlockBlockUser"></activity>
        <activity android:name=".activity.SearchFilterActivity" />
        <activity android:name=".activity.HomeActivity" />
        <activity android:name=".activity.StartUpActivity" />

        <activity android:name=".activity.LogInActivity" />
        <activity android:name=".activity.MatchingDetailActivity"></activity>
        <activity android:name=".activity.FAQActivity" android:theme="@style/HaveToolBar">
        </activity>
    </application>

</manifest>